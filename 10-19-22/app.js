/*return the average of the goveb array rounded down to the nearest integer
array will never be empty

function getAverage(arr){
    //loop through array to get sum
    return Math.floor( let sum = arr.reduce((a, b)=>a+b) /arr.length)
    //avg => sum / arr.length
    //return
    

}*/
const getAverage = arr => Math.floor( let sum = arr.reduce((a, b)=>a+b) /arr.length)