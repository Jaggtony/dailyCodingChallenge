//Simple, remove the spaces from the string, then return the resultant string.

const noSpace=(x)=>  x.split(' ').join('')
//split the string at the spaces to turn to 
//an array then joing to turn array back into string but with no space in brackets so there isnt a space in the return